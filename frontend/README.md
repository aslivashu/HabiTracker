Frontend for HabiTrack

Install dependencies:

```powershell
cd frontend
npm install
```

Start dev server:

```powershell
npm run dev
```

Note: Tailwind is referenced in `tailwind.config.js`. After `npm install`, run the Vite dev server which will pick up Tailwind if packages are installed.
